# Hitendra S — Personal Engineering Portfolio
A high-performance minimalist portfolio and UI craft studio built with React 19, Vite, and TanStack Router.
To run locally, execute `npm install` followed by `npm run dev`.
